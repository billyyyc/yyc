;(function($){

	$.fn.plugin=function(options){
		var defaults = {
			//各种参数,属性
		}
		var options = $.extend(defaults,options);
		
		this.each(function() {
			// 实现功能的代码
		})
		return this;//实现链式操作
	}

})(jQuery);